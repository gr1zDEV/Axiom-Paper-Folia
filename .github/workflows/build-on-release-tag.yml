name: Build Plugin on Release Tag

on:
  push:
    tags:
      - 'v*'
      - 'release-*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Grant execute permission for Gradle wrapper
        run: chmod +x gradlew

      - name: Build shaded plugin JAR
        run: ./gradlew --no-daemon clean shadowJar

      - name: Upload plugin artifact
        uses: actions/upload-artifact@v4
        with:
          name: Axiom-Paper-Folia-${{ github.ref_name }}
          path: build/libs/*.jar
