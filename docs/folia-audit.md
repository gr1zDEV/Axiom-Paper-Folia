# Folia Audit

| Component | File | Current Callsite | Thread Assumption | Required Folia Scheduler | Status |
|---|---|---|---|---|---|
| Legacy Bukkit scheduler usage | `src/main/java/com/moulberry/axiom/AxiomPaper.java` | `onEnable`: `Bukkit.getScheduler().scheduleSyncRepeatingTask(this, this::tick, 1, 1)` | Assumes a single global main thread tick loop. | `Bukkit.getGlobalRegionScheduler().runAtFixedRate(...)` (or equivalent Folia global task wrapper). | **Needs migration** |
| BukkitRunnable usage scan | `src/main/java/**` | No `BukkitRunnable` usage found by grep scan. | N/A | N/A | **Clear (no usage found)** |
| Packet dispatch threading | `src/main/java/com/moulberry/axiom/packet/AxiomBigPayloadHandler.java` | `decode`: non-async handlers are re-scheduled with `server.execute(...)`; `handleAsync()` handlers run directly on Netty decode thread. | Mix of Netty thread + server thread assumptions; not region-aware. | Route world/entity operations to `RegionScheduler` / `EntityScheduler`; reserve global scheduler for global-only work. | **Needs migration / review** |
| Async packet handler (`set_buffer`) | `src/main/java/com/moulberry/axiom/packet/impl/SetBlockBufferPacketListener.java` | `handleAsync() == true`; parses packet off-thread, then uses `server.execute(...)`; mutates chunks/biomes (`container.set`, `chunk.markUnsaved`) and queues operations. | Assumes `server.execute` main thread is safe for arbitrary world-wide edits. | `RegionScheduler` per affected chunk/region; avoid cross-region bulk mutation in one task. | **High-risk for Folia** |
| Async packet handler (`upload_blueprint`) | `src/main/java/com/moulberry/axiom/packet/impl/UploadBlueprintPacketListener.java` | `handleAsync() == true`; schedules `server.execute(...)` for registry mutation + broadcast. | Assumes main-thread global execution for plugin state updates. | Global region scheduler task (no region/world mutation, but global state + player list broadcast). | **Needs migration** |
| Direct block mutation in packet handler | `src/main/java/com/moulberry/axiom/packet/impl/SetBlockPacketListener.java` | `onReceive`: `player.level().setBlock(...)` and low-level section/block-entity writes in `setWithoutUpdates(...)`. | Assumes block writes are legal on single server thread for any location. | `RegionScheduler.execute(plugin, world, chunkX, chunkZ, ...)` for each affected region/chunk. | **High-risk for Folia** |
| Direct entity spawn in packet handler | `src/main/java/com/moulberry/axiom/packet/impl/SpawnEntityPacketListener.java` | `onReceive`: `serverLevel.tryAddFreshEntityWithPassengers(...)`, possible `discard()`. | Assumes entity creation/removal can occur inline on single server thread. | Entity/world-region scheduled execution (spawn location's region; entity scheduler for follow-up). | **High-risk for Folia** |
| Direct entity delete in packet handler | `src/main/java/com/moulberry/axiom/packet/impl/DeleteEntityPacketListener.java` | `onReceive`: `entity.remove(...)`. | Assumes entity mutation on current thread is globally safe. | `entity.getBukkitEntity().getScheduler().run(...)` (entity scheduler) or region-safe equivalent. | **High-risk for Folia** |
| Direct entity transform in packet handler | `src/main/java/com/moulberry/axiom/packet/impl/ManipulateEntityPacketListener.java` | `onReceive`: `entity.setPosRaw(...)`, rotation updates, data writes. | Assumes direct entity mutation from packet context is safe. | Entity scheduler per target entity (or teleport API that is Folia-safe). | **High-risk for Folia** |
| Player/world state mutation in packet handlers | `src/main/java/com/moulberry/axiom/packet/impl/TeleportPacketListener.java`, `SetTimePacketListener.java`, `SetGamemodePacketListener.java`, `SetFlySpeedPacketListener.java` | `player.teleport(...)`, `level.setDayTime(...)`, `setGameMode(...)`, `setFlyingSpeed(...)`. | Assumes single-threaded server context for player/world state writes. | Player/entity scheduler for player mutations; region/global scheduler for world time updates depending scope. | **Needs migration** |
| Non-thread-safe collections in plugin singleton | `src/main/java/com/moulberry/axiom/AxiomPaper.java` | Fields: `playerPermissions`, `lastPlotBoundsForPlayers`, `noPhysicalTriggerPlayers`, `availableDispatchSends`, `whitelistedEntities`, `blacklistedEntities` as `HashMap`/`HashSet` variants. | Assumes accesses occur only on one thread; however packet pipeline includes Netty-thread checks before scheduling. | Constrain access to scheduler-owned thread(s) OR replace with concurrency-safe structures + strict ownership model. | **Potential race / audit required** |
| Non-thread-safe static/singleton maps/sets | `src/main/java/com/moulberry/axiom/WorldExtension.java`, `paperapi/entity/ImplAxiomHiddenEntities.java`, `paperapi/block/ImplServerCustomBlocks.java`, `paperapi/display/ImplServerCustomDisplays.java` | Static/global `HashMap`/`HashSet` fields (`extensions`, `previousMarkerData`, `lastSentHiddenDisplays`, `byPlugin`, etc.). | Mostly assumes main-thread access; not uniformly guarded against async callers. | Ensure all mutations run in owner scheduler context (global/region/entity as appropriate) or harden with synchronization + ownership rules. | **Potential race / audit required** |
